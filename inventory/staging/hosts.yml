---
# Staging/Test Inventory
# Use this for testing playbooks before running in production

all:
  children:
    panorama:
      hosts:
        panorama-staging:
          ansible_host: 192.168.100.10  # Replace with your staging Panorama
          device_group: "DG-Staging"
          template_stack: "TS-Staging"
