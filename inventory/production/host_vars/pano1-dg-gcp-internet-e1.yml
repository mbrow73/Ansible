---
# GCP Internet-facing DG
# Inherits from: group_vars/internet_facing.yml, group_vars/cloud_environments.yml

# GCP-specific NTP (overrides group default)
baseline_ntp:
  primary_server: "metadata.google.internal"
  secondary_server: "0.pool.ntp.org"

# DG-specific security rules
baseline_security_rules:
  - name: "Block-Inbound-Internet"
    source_zone: ["untrust"]
    destination_zone: ["trust"]
    source_address: ["any"]
    destination_address: ["any"]
    application: ["any"]
    service: ["any"]
    action: "deny"
    log_end: true
    description: "Default deny from internet"

  - name: "Allow-Outbound-Web"
    source_zone: ["trust"]
    destination_zone: ["untrust"]
    source_address: ["GCP-Subnet"]
    destination_address: ["any"]
    application: ["ssl", "web-browsing"]
    service: ["application-default"]
    action: "allow"
    profile_group: "Internet-Security-Profile"
    log_end: true
    description: "Outbound internet with strict inspection"
